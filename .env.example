# Copy to .env for local/production builds. Never commit .env.
# Vite replaces %NEXT_PUBLIC_*% in index.html at build time (see vite.config.ts envPrefix).

NEXT_PUBLIC_SENTIENT_WIDGET_ORIGIN=https://your-widget-host.example
NEXT_PUBLIC_SENTIENT_INSTALL_KEY=your-install-key
