diff --git a/src/components/RoiCalculatorCta.tsx b/src/components/RoiCalculatorCta.tsx
index 6f9a16a..63531b2 100644
--- a/src/components/RoiCalculatorCta.tsx
+++ b/src/components/RoiCalculatorCta.tsx
@@ -26,9 +26,17 @@ export function RoiCalculatorCta() {
       <div className="relative z-10 mx-auto flex max-w-[1831px] justify-center">
         <Link
           to="/revenue-leak-calculator"
-          className="group inline-flex items-center justify-center rounded-full border border-[#FF8A8A]/45 bg-black px-8 py-4 text-center shadow-[0_14px_42px_rgba(255,138,138,0.12)] transition hover:-translate-y-0.5 hover:border-neon/80 hover:shadow-[0_18px_54px_rgba(111,255,0,0.16)] sm:px-10 sm:py-5"
+          className="ai-rainbow-cta group inline-flex items-center justify-center rounded-full px-8 py-4 text-center transition hover:-translate-y-0.5 sm:px-10 sm:py-5"
         >
-          <span className="font-grotesk whitespace-nowrap text-[15px] uppercase tracking-wide text-[#FF8A8A] transition group-hover:text-neon sm:text-[18px]">
+          <span className="ai-rainbow-cta__sparkles" aria-hidden="true">
+            <span />
+            <span />
+            <span />
+            <span />
+            <span />
+            <span />
+          </span>
+          <span className="ai-rainbow-cta__label font-grotesk whitespace-nowrap text-[15px] uppercase tracking-wide sm:text-[18px]">
             Estimate recoverable demos in the last 30 days.
           </span>
         </Link>
diff --git a/src/index.css b/src/index.css
index ebcf5c2..b0096a1 100644
--- a/src/index.css
+++ b/src/index.css
@@ -122,6 +122,160 @@
   text-shadow: 0 0 18px rgba(255, 138, 138, 0.42);
 }
 
+.ai-rainbow-cta {
+  position: relative;
+  isolation: isolate;
+  overflow: hidden;
+  border: 1px solid transparent;
+  color: #f1f6ff;
+  background:
+    linear-gradient(#02030d, #02030d) padding-box,
+    conic-gradient(
+      from var(--ai-rainbow-spin, 0deg),
+      #ff8a8a,
+      #ffce5c,
+      #6fff00,
+      #55d6ff,
+      #9f7cff,
+      #ff8a8a
+    )
+    border-box;
+  box-shadow:
+    0 0 0 1px rgba(255, 255, 255, 0.08),
+    0 18px 64px rgba(111, 255, 0, 0.18),
+    0 14px 52px rgba(255, 138, 138, 0.16),
+    inset 0 1px rgba(255, 255, 255, 0.18);
+  transform: translateZ(0);
+  animation: ai-rainbow-border-spin 4.8s linear infinite;
+}
+
+.ai-rainbow-cta::before {
+  content: '';
+  position: absolute;
+  inset: 1px;
+  z-index: -2;
+  border-radius: inherit;
+  background:
+    radial-gradient(circle at 18% 22%, rgba(255, 138, 138, 0.32), transparent 28%),
+    radial-gradient(circle at 76% 28%, rgba(85, 214, 255, 0.28), transparent 30%),
+    radial-gradient(circle at 54% 92%, rgba(111, 255, 0, 0.26), transparent 34%),
+    linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.02));
+  filter: saturate(1.22);
+}
+
+.ai-rainbow-cta::after {
+  content: '';
+  position: absolute;
+  inset: -40% -22%;
+  z-index: -1;
+  background:
+    linear-gradient(
+      105deg,
+      transparent 0%,
+      transparent 35%,
+      rgba(255, 255, 255, 0.64) 47%,
+      rgba(111, 255, 0, 0.45) 52%,
+      transparent 66%,
+      transparent 100%
+    );
+  opacity: 0.58;
+  transform: translateX(-58%) rotate(6deg);
+  animation: ai-rainbow-sheen 2.8s cubic-bezier(0.72, 0, 0.18, 1) infinite;
+}
+
+.ai-rainbow-cta__label {
+  position: relative;
+  z-index: 2;
+  color: #f1f6ff;
+  text-shadow:
+    0 0 18px rgba(255, 255, 255, 0.32),
+    0 0 30px rgba(111, 255, 0, 0.24);
+  transition:
+    color 180ms ease,
+    text-shadow 180ms ease;
+}
+
+.ai-rainbow-cta:hover .ai-rainbow-cta__label {
+  color: #6fff00;
+  text-shadow:
+    0 0 18px rgba(111, 255, 0, 0.72),
+    0 0 38px rgba(85, 214, 255, 0.32);
+}
+
+.ai-rainbow-cta__sparkles {
+  pointer-events: none;
+  position: absolute;
+  inset: 0;
+  z-index: 1;
+  overflow: hidden;
+  border-radius: inherit;
+}
+
+.ai-rainbow-cta__sparkles span {
+  position: absolute;
+  left: var(--spark-x);
+  top: var(--spark-y);
+  width: var(--spark-size);
+  height: var(--spark-size);
+  border-radius: 999px;
+  background: var(--spark-color);
+  box-shadow:
+    0 0 10px var(--spark-color),
+    0 0 22px rgba(255, 255, 255, 0.22);
+  opacity: 0;
+  transform: translate3d(0, 0, 0) scale(0.4);
+  animation: ai-rainbow-spark 1.9s ease-in-out infinite;
+  animation-delay: var(--spark-delay);
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(1) {
+  --spark-x: 12%;
+  --spark-y: 26%;
+  --spark-size: 4px;
+  --spark-color: #ff8a8a;
+  --spark-delay: -0.1s;
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(2) {
+  --spark-x: 28%;
+  --spark-y: 70%;
+  --spark-size: 3px;
+  --spark-color: #ffce5c;
+  --spark-delay: -0.55s;
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(3) {
+  --spark-x: 48%;
+  --spark-y: 18%;
+  --spark-size: 4px;
+  --spark-color: #6fff00;
+  --spark-delay: -0.95s;
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(4) {
+  --spark-x: 66%;
+  --spark-y: 74%;
+  --spark-size: 3px;
+  --spark-color: #55d6ff;
+  --spark-delay: -1.25s;
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(5) {
+  --spark-x: 80%;
+  --spark-y: 30%;
+  --spark-size: 4px;
+  --spark-color: #9f7cff;
+  --spark-delay: -1.55s;
+}
+
+.ai-rainbow-cta__sparkles span:nth-child(6) {
+  --spark-x: 92%;
+  --spark-y: 62%;
+  --spark-size: 3px;
+  --spark-color: #f1f6ff;
+  --spark-delay: -1.8s;
+}
+
 @keyframes leak-rate-flash {
   0%,
   100% {
@@ -135,6 +289,43 @@
   }
 }
 
+@property --ai-rainbow-spin {
+  syntax: '<angle>';
+  inherits: false;
+  initial-value: 0deg;
+}
+
+@keyframes ai-rainbow-border-spin {
+  to {
+    --ai-rainbow-spin: 360deg;
+  }
+}
+
+@keyframes ai-rainbow-sheen {
+  0%,
+  42% {
+    transform: translateX(-58%) rotate(6deg);
+  }
+
+  70%,
+  100% {
+    transform: translateX(58%) rotate(6deg);
+  }
+}
+
+@keyframes ai-rainbow-spark {
+  0%,
+  100% {
+    opacity: 0;
+    transform: translate3d(0, 8px, 0) scale(0.42);
+  }
+
+  45% {
+    opacity: 0.95;
+    transform: translate3d(0, -4px, 0) scale(1);
+  }
+}
+
 .funnel-segment--0 {
   clip-path: polygon(0 0, 92% 0, 100% 50%, 92% 100%, 0 100%);
   max-width: 100%;
@@ -414,6 +605,12 @@
     animation: none;
   }
 
+  .ai-rainbow-cta,
+  .ai-rainbow-cta::after,
+  .ai-rainbow-cta__sparkles span {
+    animation: none;
+  }
+
   .blackhole-profit-suck span {
     animation: none;
   }
