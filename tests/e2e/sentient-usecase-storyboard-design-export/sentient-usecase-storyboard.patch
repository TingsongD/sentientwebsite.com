diff --git a/src/index.css b/src/index.css
index f5309ab..dcb5b7c 100644
--- a/src/index.css
+++ b/src/index.css
@@ -539,6 +539,430 @@
   align-content: space-between;
 }
 
+.use-case-storyboard {
+  position: relative;
+  overflow: hidden;
+  border: 1px solid rgba(255, 255, 255, 0.1);
+  border-radius: 28px;
+  background:
+    radial-gradient(circle at 12% 0%, rgba(111, 255, 0, 0.12), transparent 18rem),
+    radial-gradient(circle at 84% 12%, rgba(85, 214, 255, 0.08), transparent 20rem),
+    rgba(255, 255, 255, 0.035);
+  box-shadow:
+    inset 0 1px rgba(255, 255, 255, 0.12),
+    0 28px 88px rgba(0, 0, 0, 0.18);
+  padding: clamp(1rem, 2vw, 1.35rem);
+}
+
+.use-case-storyboard::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  background-image:
+    linear-gradient(rgba(241, 246, 255, 0.035) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(241, 246, 255, 0.035) 1px, transparent 1px);
+  background-size: 46px 46px;
+  mask-image: radial-gradient(circle at 50% 45%, #000, transparent 76%);
+}
+
+.use-case-storyboard__header {
+  position: relative;
+  z-index: 1;
+  display: flex;
+  flex-wrap: wrap;
+  align-items: end;
+  justify-content: space-between;
+  gap: 0.75rem 1.5rem;
+  margin-bottom: 1rem;
+}
+
+.use-case-storyboard__header p:last-child {
+  max-width: 34rem;
+}
+
+.use-case-storyboard__panels {
+  position: relative;
+  z-index: 1;
+  display: grid;
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+  gap: 0.85rem;
+  list-style: none;
+  margin: 0;
+  padding: 0;
+}
+
+.use-case-storyboard__panel {
+  min-width: 0;
+}
+
+.use-case-storyboard__browser {
+  display: grid;
+  grid-template-rows: auto minmax(0, 1fr);
+  min-height: 320px;
+  overflow: hidden;
+  border: 1px solid rgba(241, 246, 255, 0.14);
+  border-radius: 22px;
+  background:
+    linear-gradient(135deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0.018)),
+    rgba(1, 4, 18, 0.9);
+  box-shadow:
+    inset 0 1px rgba(255, 255, 255, 0.12),
+    0 20px 54px rgba(0, 0, 0, 0.24);
+  transition:
+    border-color 180ms ease,
+    transform 180ms ease,
+    box-shadow 180ms ease;
+}
+
+.use-case-storyboard__panel:hover .use-case-storyboard__browser {
+  border-color: rgba(111, 255, 0, 0.38);
+  box-shadow:
+    inset 0 1px rgba(255, 255, 255, 0.16),
+    0 24px 68px rgba(111, 255, 0, 0.08);
+  transform: translateY(-3px);
+}
+
+.use-case-storyboard__bar {
+  display: grid;
+  grid-template-columns: repeat(3, 8px) minmax(0, 1fr);
+  align-items: center;
+  gap: 6px;
+  min-height: 38px;
+  border-bottom: 1px solid rgba(241, 246, 255, 0.08);
+  padding: 0 12px;
+  background: rgba(255, 255, 255, 0.035);
+}
+
+.use-case-storyboard__bar span {
+  width: 8px;
+  height: 8px;
+  border-radius: 999px;
+  background: rgba(241, 246, 255, 0.25);
+}
+
+.use-case-storyboard__bar span:first-child {
+  background: #ff8a8a;
+}
+
+.use-case-storyboard__bar span:nth-child(2) {
+  background: #ffce5c;
+}
+
+.use-case-storyboard__bar span:nth-child(3) {
+  background: #6fff00;
+}
+
+.use-case-storyboard__bar p {
+  min-width: 0;
+  margin: 0 0 0 6px;
+  overflow: hidden;
+  color: rgba(241, 246, 255, 0.5);
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 10px;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.use-case-storyboard__screen {
+  position: relative;
+  min-height: 282px;
+  overflow: hidden;
+  padding: 16px;
+}
+
+.use-case-storyboard__screen::after {
+  content: '';
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  background: radial-gradient(circle at 50% 52%, transparent, rgba(1, 4, 18, 0.28) 72%);
+}
+
+.use-case-storyboard__pricing,
+.use-case-storyboard__preview,
+.use-case-storyboard__qualification,
+.use-case-storyboard__handoff {
+  position: relative;
+  z-index: 1;
+  display: grid;
+  height: 100%;
+  min-height: 250px;
+  gap: 12px;
+}
+
+.use-case-storyboard__pricing {
+  grid-template-columns: minmax(0, 1fr);
+  align-content: space-between;
+}
+
+.use-case-storyboard__mock-copy {
+  border: 1px solid rgba(241, 246, 255, 0.11);
+  border-radius: 18px;
+  padding: 16px;
+  background: rgba(255, 255, 255, 0.04);
+}
+
+.use-case-storyboard__mock-copy p {
+  margin: 0 0 10px;
+  color: #6fff00;
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 9px;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+}
+
+.use-case-storyboard__mock-copy strong {
+  display: block;
+  color: #f1f6ff;
+  font-family: Anton, Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
+  font-size: clamp(1.65rem, 2.7vw, 2.55rem);
+  font-weight: 900;
+  line-height: 1;
+  text-transform: uppercase;
+}
+
+.use-case-storyboard__mock-copy span {
+  display: block;
+  height: 8px;
+  margin-top: 12px;
+  border-radius: 999px;
+  background: rgba(241, 246, 255, 0.11);
+}
+
+.use-case-storyboard__mock-copy span:nth-of-type(2) {
+  width: 74%;
+}
+
+.use-case-storyboard__mock-copy span:nth-of-type(3) {
+  width: 52%;
+}
+
+.use-case-storyboard__beacon {
+  display: grid;
+  min-height: 110px;
+  place-items: center;
+  border: 1px solid rgba(111, 255, 0, 0.16);
+  border-radius: 18px;
+  background: radial-gradient(circle, rgba(111, 255, 0, 0.16), transparent 64%);
+}
+
+.use-case-storyboard__beacon span {
+  position: relative;
+  width: 82px;
+  aspect-ratio: 1;
+  border: 1px solid rgba(111, 255, 0, 0.35);
+  border-radius: 999px;
+  box-shadow: 0 0 42px rgba(111, 255, 0, 0.16);
+}
+
+.use-case-storyboard__beacon span::before,
+.use-case-storyboard__beacon span::after {
+  content: '';
+  position: absolute;
+  border-radius: inherit;
+}
+
+.use-case-storyboard__beacon span::before {
+  inset: 22%;
+  border: 1px solid rgba(111, 255, 0, 0.32);
+}
+
+.use-case-storyboard__beacon span::after {
+  inset: 42%;
+  background: #6fff00;
+  box-shadow: 0 0 28px rgba(111, 255, 0, 0.72);
+}
+
+.use-case-storyboard__preview {
+  grid-template-rows: minmax(7.5rem, 1fr) auto;
+}
+
+.use-case-storyboard__play {
+  position: relative;
+  overflow: hidden;
+  border: 1px solid rgba(241, 246, 255, 0.12);
+  border-radius: 18px;
+  background:
+    radial-gradient(circle at 48% 48%, rgba(111, 255, 0, 0.22), transparent 5rem),
+    linear-gradient(135deg, rgba(255, 138, 138, 0.18), rgba(85, 214, 255, 0.08)),
+    rgba(255, 255, 255, 0.04);
+}
+
+.use-case-storyboard__play::before {
+  content: '';
+  position: absolute;
+  left: 50%;
+  top: 50%;
+  width: 58px;
+  height: 58px;
+  border: 1px solid rgba(241, 246, 255, 0.34);
+  border-radius: 999px;
+  background: rgba(1, 4, 18, 0.72);
+  transform: translate(-50%, -50%);
+}
+
+.use-case-storyboard__play::after {
+  content: '';
+  position: absolute;
+  left: calc(50% + 4px);
+  top: 50%;
+  border-top: 11px solid transparent;
+  border-bottom: 11px solid transparent;
+  border-left: 17px solid #6fff00;
+  transform: translate(-50%, -50%);
+}
+
+.use-case-storyboard__chat {
+  display: grid;
+  gap: 8px;
+}
+
+.use-case-storyboard__chat p {
+  margin: 0;
+  border: 1px solid rgba(85, 214, 255, 0.16);
+  border-radius: 14px;
+  padding: 10px 11px;
+  background: rgba(85, 214, 255, 0.055);
+  color: rgba(241, 246, 255, 0.72);
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 10px;
+  line-height: 1.45;
+}
+
+.use-case-storyboard__chat p:last-child {
+  border-color: rgba(111, 255, 0, 0.2);
+  background: rgba(111, 255, 0, 0.075);
+}
+
+.use-case-storyboard__qualification {
+  grid-template-rows: auto minmax(6.5rem, 1fr);
+}
+
+.use-case-storyboard__fields {
+  display: grid;
+  gap: 8px;
+}
+
+.use-case-storyboard__fields span,
+.use-case-storyboard__crm span {
+  display: grid;
+  gap: 4px;
+  border: 1px solid rgba(241, 246, 255, 0.1);
+  border-radius: 12px;
+  padding: 9px 10px;
+  background: rgba(255, 255, 255, 0.04);
+  color: rgba(241, 246, 255, 0.78);
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 10px;
+  line-height: 1.35;
+}
+
+.use-case-storyboard__fields b,
+.use-case-storyboard__crm b {
+  color: rgba(111, 255, 0, 0.82);
+  font-size: 9px;
+  font-weight: 500;
+  letter-spacing: 0.1em;
+  text-transform: uppercase;
+}
+
+.use-case-storyboard__score {
+  display: grid;
+  width: 108px;
+  aspect-ratio: 1;
+  place-self: center;
+  place-items: center;
+  border-radius: 999px;
+  background:
+    radial-gradient(circle at center, #020a26 0 53%, transparent 54%),
+    conic-gradient(#6fff00 0 82%, rgba(241, 246, 255, 0.1) 82% 100%);
+  color: #6fff00;
+  font-family: Anton, Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
+  font-size: 2.4rem;
+  line-height: 1;
+}
+
+.use-case-storyboard__handoff {
+  grid-template-rows: auto minmax(0, 1fr);
+}
+
+.use-case-storyboard__calendar {
+  border: 1px solid rgba(111, 255, 0, 0.24);
+  border-radius: 18px;
+  padding: 15px;
+  background: rgba(111, 255, 0, 0.075);
+}
+
+.use-case-storyboard__calendar span,
+.use-case-storyboard__calendar small {
+  display: block;
+  color: rgba(241, 246, 255, 0.62);
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 10px;
+  letter-spacing: 0.1em;
+  text-transform: uppercase;
+}
+
+.use-case-storyboard__calendar strong {
+  display: block;
+  margin-top: 10px;
+  color: #6fff00;
+  font-family: Anton, Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
+  font-size: clamp(1.65rem, 3vw, 2.25rem);
+  font-weight: 900;
+  line-height: 1;
+  text-transform: uppercase;
+}
+
+.use-case-storyboard__calendar small {
+  margin-top: 8px;
+}
+
+.use-case-storyboard__crm {
+  display: grid;
+  gap: 8px;
+}
+
+.use-case-storyboard__caption {
+  margin-top: 0.85rem;
+  border-left: 1px solid rgba(111, 255, 0, 0.36);
+  padding-left: 0.85rem;
+}
+
+.use-case-storyboard__number {
+  display: inline-grid;
+  width: 1.65rem;
+  height: 1.65rem;
+  place-items: center;
+  border-radius: 999px;
+  background: #6fff00;
+  color: #020a26;
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
+  font-size: 0.7rem;
+  margin-bottom: 0.75rem;
+}
+
+@media (max-width: 1180px) {
+  .use-case-storyboard__panels {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+  }
+}
+
+@media (max-width: 700px) {
+  .use-case-storyboard {
+    border-radius: 22px;
+  }
+
+  .use-case-storyboard__panels {
+    grid-template-columns: 1fr;
+  }
+
+  .use-case-storyboard__browser {
+    min-height: 290px;
+  }
+}
+
 @media (min-width: 1024px) {
   .blackhole-leak-card {
     min-height: 380px;
diff --git a/src/pages/HomePage.tsx b/src/pages/HomePage.tsx
index 826f89b..7e2b147 100644
--- a/src/pages/HomePage.tsx
+++ b/src/pages/HomePage.tsx
@@ -175,6 +175,104 @@ const REVENUE_RECOVERY_USE_CASES = [
 
 type RevenueRecoveryUseCase = (typeof REVENUE_RECOVERY_USE_CASES)[number]
 
+type UseCaseStoryboardContext = {
+  url: string
+  previewUrl: string
+  persona: string
+  buyerQuestion: string
+  aiAnswer: string
+  role: string
+  useCase: string
+  timeline: string
+  stack: string
+  score: string
+  calendarTitle: string
+  crmRows: ReadonlyArray<{ label: string; value: string }>
+}
+
+const USE_CASE_STORYBOARD_CONTEXT = {
+  'demo-recovery': {
+    url: 'seosuite.example/pricing',
+    previewUrl: 'seosuite.example/demo-preview',
+    persona: 'SEO SaaS buyer',
+    buyerQuestion: 'Does this fit our CRM and reporting stack?',
+    aiAnswer:
+      'Yes. Here is the approved integration path and the 90-second workflow for agency SEO teams.',
+    role: 'VP Marketing',
+    useCase: 'Recover demo-ready pricing traffic',
+    timeline: 'This quarter',
+    stack: 'HubSpot, Webflow, Calendly',
+    score: '82',
+    calendarTitle: 'Demo booked',
+    crmRows: [
+      { label: 'Pages', value: 'Pricing, comparison, security' },
+      { label: 'Need', value: 'Recover demo-ready traffic' },
+      { label: 'Objection', value: 'CRM and reporting stack' },
+      { label: 'Opener', value: 'Lead with agency SEO workflow' },
+    ],
+  },
+  'failed-payment-recovery': {
+    url: 'seosuite.example/billing/retry',
+    previewUrl: 'seosuite.example/save-path',
+    persona: 'At-risk customer',
+    buyerQuestion: 'Can I fix billing without losing my workspace?',
+    aiAnswer:
+      'Yes. Here is the approved retry path, plan reminder, and owner escalation rule for this account.',
+    role: 'Account owner',
+    useCase: 'Resolve payment friction before churn',
+    timeline: 'Before renewal closes',
+    stack: 'Stripe, HubSpot, email',
+    score: '76',
+    calendarTitle: 'Save path opened',
+    crmRows: [
+      { label: 'Signal', value: 'Failed charge and cancel intent' },
+      { label: 'Reason', value: 'Billing friction, not product rejection' },
+      { label: 'Action', value: 'Retry path plus owner task' },
+      { label: 'Outcome', value: 'Save, retry, or write-off synced' },
+    ],
+  },
+  'no-show-recovery': {
+    url: 'seosuite.example/calendar/demo',
+    previewUrl: 'seosuite.example/reschedule',
+    persona: 'No-show prospect',
+    buyerQuestion: 'Can we pick this back up without starting over?',
+    aiAnswer:
+      'Yes. Here is the original buying context, the reason you booked, and a low-friction reschedule path.',
+    role: 'Marketing leader',
+    useCase: 'Recover missed demo from high intent',
+    timeline: 'Same week',
+    stack: 'Calendly, HubSpot, email',
+    score: '79',
+    calendarTitle: 'Demo rescheduled',
+    crmRows: [
+      { label: 'Context', value: 'Original page path and reason booked' },
+      { label: 'Reminder', value: 'References comparison-page concern' },
+      { label: 'Route', value: 'Reschedule plus owner alert' },
+      { label: 'Next', value: 'Sales opens with preserved context' },
+    ],
+  },
+  'buyer-insights': {
+    url: 'seosuite.example/security',
+    previewUrl: 'seosuite.example/revops-review',
+    persona: 'Revenue team',
+    buyerQuestion: 'Why do qualified buyers hesitate before booking?',
+    aiAnswer:
+      'The pattern is implementation risk on security and pricing pages, concentrated among mid-market buyers.',
+    role: 'Founder / RevOps',
+    useCase: 'Find recurring buyer hesitation',
+    timeline: 'Weekly review',
+    stack: 'HubSpot, analytics, website',
+    score: '91',
+    calendarTitle: 'Repair work queued',
+    crmRows: [
+      { label: 'Pattern', value: 'Security and implementation objections' },
+      { label: 'Segment', value: 'Mid-market buyers comparing vendors' },
+      { label: 'Repair', value: 'Page copy and playbook update' },
+      { label: 'Review', value: 'Weekly founder and RevOps agenda' },
+    ],
+  },
+} as const satisfies Record<RevenueRecoveryUseCase['id'], UseCaseStoryboardContext>
+
 type LeakClockEstimate = {
   usAnnualLeakUsd: number
   typicalAnnualLeakUsd: number
@@ -388,37 +486,129 @@ function IntegrationLogoStrip() {
   )
 }
 
-function UseCaseFlowDiagram({ useCase }: { useCase: RevenueRecoveryUseCase }) {
+function UseCaseStoryboardVisual({
+  useCase,
+  stepIndex,
+}: {
+  useCase: RevenueRecoveryUseCase
+  stepIndex: number
+}) {
+  const context = USE_CASE_STORYBOARD_CONTEXT[useCase.id]
+
+  if (stepIndex === 0) {
+    return (
+      <div className="use-case-storyboard__pricing">
+        <div className="use-case-storyboard__mock-copy">
+          <p>{context.persona}</p>
+          <strong>{useCase.title}</strong>
+          <span />
+          <span />
+          <span />
+        </div>
+        <div className="use-case-storyboard__beacon" aria-hidden>
+          <span />
+        </div>
+      </div>
+    )
+  }
+
+  if (stepIndex === 1) {
+    return (
+      <div className="use-case-storyboard__preview">
+        <div className="use-case-storyboard__play" aria-hidden />
+        <div className="use-case-storyboard__chat">
+          <p>{context.buyerQuestion}</p>
+          <p>{context.aiAnswer}</p>
+        </div>
+      </div>
+    )
+  }
+
+  if (stepIndex === 2) {
+    return (
+      <div className="use-case-storyboard__qualification">
+        <div className="use-case-storyboard__fields">
+          <span>
+            <b>Role</b>
+            {context.role}
+          </span>
+          <span>
+            <b>Use case</b>
+            {context.useCase}
+          </span>
+          <span>
+            <b>Timeline</b>
+            {context.timeline}
+          </span>
+          <span>
+            <b>Stack</b>
+            {context.stack}
+          </span>
+        </div>
+        <div className="use-case-storyboard__score" aria-label={`Fit score ${context.score}`}>
+          {context.score}
+        </div>
+      </div>
+    )
+  }
+
   return (
-    <div className="rounded-[22px] border border-white/10 bg-background/55 p-4 sm:p-5">
-      <p className="font-grotesk text-[12px] uppercase tracking-wide text-neon">
-        Walkthrough diagram
-      </p>
-      <ol
-        className="mt-5 grid gap-3 md:grid-cols-4"
-        aria-label={`${useCase.title} flowchart`}
-      >
+    <div className="use-case-storyboard__handoff">
+      <div className="use-case-storyboard__calendar">
+        <span>Calendar</span>
+        <strong>{context.calendarTitle}</strong>
+        <small>Context attached</small>
+      </div>
+      <div className="use-case-storyboard__crm">
+        {context.crmRows.map((row) => (
+          <span key={row.label}>
+            <b>{row.label}</b>
+            {row.value}
+          </span>
+        ))}
+      </div>
+    </div>
+  )
+}
+
+function UseCaseStoryboard({ useCase }: { useCase: RevenueRecoveryUseCase }) {
+  const context = USE_CASE_STORYBOARD_CONTEXT[useCase.id]
+
+  return (
+    <div className="use-case-storyboard">
+      <div className="use-case-storyboard__header">
+        <p className="font-grotesk text-[12px] uppercase tracking-wide text-neon">
+          Storyboard walkthrough
+        </p>
+        <p className="font-mono text-[11px] normal-case leading-relaxed text-cream/58 sm:text-[12px]">
+          Four image-led moments show the workflow from the first signal to the recovered outcome.
+        </p>
+      </div>
+      <ol className="use-case-storyboard__panels" aria-label={`${useCase.title} storyboard`}>
         {useCase.flow.map((step, index) => (
-          <li key={step.label} className="relative">
-            <div className="h-full rounded-[18px] border border-neon/20 bg-white/[0.04] p-4">
-              <div className="font-mono mb-3 inline-flex h-7 w-7 items-center justify-center rounded-full bg-neon text-[11px] text-background">
-                {index + 1}
+          <li key={step.label} className="use-case-storyboard__panel">
+            <div className="use-case-storyboard__browser">
+              <div className="use-case-storyboard__bar">
+                <span />
+                <span />
+                <span />
+                <p>{index === 0 ? context.url : index === 1 ? context.previewUrl : step.label}</p>
               </div>
-              <h4 className="font-grotesk text-[14px] uppercase leading-tight text-cream sm:text-[15px]">
+              <div className="use-case-storyboard__screen">
+                <UseCaseStoryboardVisual useCase={useCase} stepIndex={index} />
+              </div>
+            </div>
+            <div className="use-case-storyboard__caption">
+              <span className="use-case-storyboard__number">
+                {index + 1}
+              </span>
+              <h4 className="font-grotesk text-[14px] uppercase leading-tight text-cream sm:text-[17px]">
                 {step.label}
               </h4>
               <p className="font-mono mt-2 text-[11px] normal-case leading-relaxed text-cream/62 sm:text-[12px]">
                 {step.detail}
               </p>
             </div>
-            {index < useCase.flow.length - 1 ? (
-              <div
-                className="font-mono hidden text-center text-[18px] text-neon/70 md:absolute md:-right-[1.15rem] md:top-1/2 md:block md:-translate-y-1/2"
-                aria-hidden
-              >
-                -&gt;
-              </div>
-            ) : null}
           </li>
         ))}
       </ol>
@@ -480,7 +670,7 @@ function UseCaseDetailSection({
         </div>
 
         <div className="mt-8">
-          <UseCaseFlowDiagram useCase={useCase} />
+          <UseCaseStoryboard useCase={useCase} />
         </div>
 
         {children}
